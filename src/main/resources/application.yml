spring:
  application:
    name: LibDev

  datasource:
    url: jdbc:mysql://localhost:3306/library
    username: master
    password: password
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
